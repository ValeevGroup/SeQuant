# CLAUDE.md

Agent-facing conventions live in `AGENTS.md`; this file only imports them.
Keep the content there, not here. (A regular file rather than a symlink so
checkouts without symlink support still get the content.)

@AGENTS.md
