section test_section:
R2u{a1,a2;i1,i2} = A{a1;i1} A{a2;i2}
R2{a1,a2;i1,i2} = R2u{a1,a2;i1,i2} + R2u{a2,a1;i1,i2}

=================================================
Plain Text
=================================================
Declare index i_1
Declare index i_2
Declare index a_1
Declare index a_2

section test_section(tensor A[a_1, i_1], tensor R2[a_1, a_2, i_1, i_2], tensor R2u[a_1, a_2, i_1, i_2])
  Create R2u[a_1, a_2, i_1, i_2] and initialize to zero
  Load A[a_1, i_1]
  Compute R2u[a_1, a_2, i_1, i_2] += A[a_1, i_1] A[a_2, i_2]
  Unload A[a_1, i_1]
  Persist R2u[a_1, a_2, i_1, i_2]

  Create R2[a_1, a_2, i_1, i_2] and initialize to zero
  Load R2u[a_1, a_2, i_1, i_2]
  Compute R2[a_1, a_2, i_1, i_2] += R2u[a_1, a_2, i_1, i_2]
  Compute R2[a_1, a_2, i_1, i_2] += R2u[a_2, a_1, i_1, i_2]
  Unload R2u[a_1, a_2, i_1, i_2]
  Persist R2[a_1, a_2, i_1, i_2]
end section

=================================================
C (TAPP)
=================================================
/* Generated by SeQuant */

#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include <tapp.h>

typedef enum {
  SEQUANT_SUCCESS = 0,
  SEQUANT_TAPP_FAILURE,
  SEQUANT_OUT_OF_MEMORY,
  SEQUANT_IO_FAILURE
} sequant_status;

/* Caller-provided storage of tensors and variables, identified by name. Both
   callbacks transfer count elements and return 0 on success. */
typedef struct {
  int (*load)(void *user, const char *name, double *buffer, size_t count);
  int (*store)(void *user, const char *name, const double *buffer, size_t count);
  void *user;
} sequant_io;

/* Mode labels */
enum {
  SEQUANT_IDX_i_1,
  SEQUANT_IDX_i_2,
  SEQUANT_IDX_a_1,
  SEQUANT_IDX_a_2
};

/* Extents of the index spaces */
typedef struct {
  int64_t nocc;
  int64_t nvirt;
} sequant_dims;

typedef struct {
  sequant_dims dims;
  TAPP_handle handle;
  TAPP_executor exec;
  /* Layout and number of elements of each tensor block */
  TAPP_tensor_info info_scalar;
  size_t size_scalar;
  TAPP_tensor_info info_vo;
  size_t size_vo;
  TAPP_tensor_info info_vvoo;
  size_t size_vvoo;
  /* Tensor operations */
  TAPP_tensor_product plan_1; /* vvoo[a_1, a_2, i_1, i_2] += vo[a_1, i_1] vo[a_2, i_2] */
  TAPP_tensor_product plan_2; /* vvoo[a_1, a_2, i_1, i_2] += vvoo[a_1, a_2, i_1, i_2] */
  TAPP_tensor_product plan_3; /* vvoo[a_1, a_2, i_1, i_2] += vvoo[a_2, a_1, i_1, i_2] */
} sequant_plans;

/* Error handling: the macros below expect the enclosing function to have the
   variables status and error as well as a cleanup label. */
#define SEQUANT_CHECK(call) \
  do { \
    TAPP_error tapp_error_ = (call); \
    if (!TAPP_check_success(tapp_error_)) { \
      if (error) *error = tapp_error_; \
      status = SEQUANT_TAPP_FAILURE; \
      goto cleanup; \
    } \
  } while (0)

#define SEQUANT_ALLOCATE(buffer, count) \
  do { \
    (buffer) = malloc((count) * sizeof *(buffer)); \
    if (!(buffer)) { \
      status = SEQUANT_OUT_OF_MEMORY; \
      goto cleanup; \
    } \
  } while (0)

#define SEQUANT_ALLOCATE_ZERO(buffer, count) \
  do { \
    (buffer) = calloc((count), sizeof *(buffer)); \
    if (!(buffer)) { \
      status = SEQUANT_OUT_OF_MEMORY; \
      goto cleanup; \
    } \
  } while (0)

#define SEQUANT_LOAD(name, buffer, count) \
  do { \
    if (io->load(io->user, (name), (buffer), (count)) != 0) { \
      status = SEQUANT_IO_FAILURE; \
      goto cleanup; \
    } \
  } while (0)

#define SEQUANT_STORE(name, buffer, count) \
  do { \
    if (io->store(io->user, (name), (buffer), (count)) != 0) { \
      status = SEQUANT_IO_FAILURE; \
      goto cleanup; \
    } \
  } while (0)

/* Computes the strides of a dense row-major tensor and returns its number of elements */
static size_t sequant_layout(int nmode, const int64_t *extents, int64_t *strides)
{
  size_t size = 1;
  for (int i = nmode - 1; i >= 0; --i) {
    strides[i] = (int64_t)size;
    size *= (size_t)extents[i];
  }
  return size;
}

void sequant_destroy_plans(sequant_plans *plans);

/* Creates all TAPP objects needed by the compute functions. On failure, nothing
   needs to be destroyed and, unless error is NULL, *error holds the failing TAPP
   error code. */
sequant_status sequant_create_plans(sequant_plans *plans, const sequant_dims *dims, TAPP_error *error)
{
  sequant_status status = SEQUANT_SUCCESS;

  memset(plans, 0, sizeof *plans);
  plans->dims = *dims;
  SEQUANT_CHECK(TAPP_create_handle(&plans->handle));
  SEQUANT_CHECK(TAPP_create_executor(&plans->exec));

  SEQUANT_CHECK(TAPP_create_tensor_info(&plans->info_scalar, TAPP_F64, 0, NULL, NULL));
  plans->size_scalar = 1;

  {
    const int64_t extents[2] = {dims->nvirt, dims->nocc};
    int64_t strides[2];
    plans->size_vo = sequant_layout(2, extents, strides);
    SEQUANT_CHECK(TAPP_create_tensor_info(&plans->info_vo, TAPP_F64, 2, extents, strides));
  }

  {
    const int64_t extents[4] = {dims->nvirt, dims->nvirt, dims->nocc, dims->nocc};
    int64_t strides[4];
    plans->size_vvoo = sequant_layout(4, extents, strides);
    SEQUANT_CHECK(TAPP_create_tensor_info(&plans->info_vvoo, TAPP_F64, 4, extents, strides));
  }

  {
    /* vvoo[a_1, a_2, i_1, i_2] += vo[a_1, i_1] vo[a_2, i_2] */
    static const int64_t idx_A[] = {SEQUANT_IDX_a_1, SEQUANT_IDX_i_1};
    static const int64_t idx_B[] = {SEQUANT_IDX_a_2, SEQUANT_IDX_i_2};
    static const int64_t idx_D[] = {SEQUANT_IDX_a_1, SEQUANT_IDX_a_2, SEQUANT_IDX_i_1, SEQUANT_IDX_i_2};
    SEQUANT_CHECK(TAPP_create_tensor_product(&plans->plan_1, plans->handle,
                                             TAPP_IDENTITY, plans->info_vo, idx_A,
                                             TAPP_IDENTITY, plans->info_vo, idx_B,
                                             TAPP_IDENTITY, plans->info_vvoo, idx_D,
                                             TAPP_IDENTITY, plans->info_vvoo, idx_D,
                                             TAPP_DEFAULT_PREC));
  }

  {
    /* vvoo[a_1, a_2, i_1, i_2] += vvoo[a_1, a_2, i_1, i_2] */
    static const int64_t idx_A[] = {SEQUANT_IDX_a_1, SEQUANT_IDX_a_2, SEQUANT_IDX_i_1, SEQUANT_IDX_i_2};
    static const int64_t idx_D[] = {SEQUANT_IDX_a_1, SEQUANT_IDX_a_2, SEQUANT_IDX_i_1, SEQUANT_IDX_i_2};
    SEQUANT_CHECK(TAPP_create_tensor_product(&plans->plan_2, plans->handle,
                                             TAPP_IDENTITY, plans->info_vvoo, idx_A,
                                             TAPP_IDENTITY, plans->info_scalar, NULL,
                                             TAPP_IDENTITY, plans->info_vvoo, idx_D,
                                             TAPP_IDENTITY, plans->info_vvoo, idx_D,
                                             TAPP_DEFAULT_PREC));
  }

  {
    /* vvoo[a_1, a_2, i_1, i_2] += vvoo[a_2, a_1, i_1, i_2] */
    static const int64_t idx_A[] = {SEQUANT_IDX_a_2, SEQUANT_IDX_a_1, SEQUANT_IDX_i_1, SEQUANT_IDX_i_2};
    static const int64_t idx_D[] = {SEQUANT_IDX_a_1, SEQUANT_IDX_a_2, SEQUANT_IDX_i_1, SEQUANT_IDX_i_2};
    SEQUANT_CHECK(TAPP_create_tensor_product(&plans->plan_3, plans->handle,
                                             TAPP_IDENTITY, plans->info_vvoo, idx_A,
                                             TAPP_IDENTITY, plans->info_scalar, NULL,
                                             TAPP_IDENTITY, plans->info_vvoo, idx_D,
                                             TAPP_IDENTITY, plans->info_vvoo, idx_D,
                                             TAPP_DEFAULT_PREC));
  }

  return status;

cleanup:
  sequant_destroy_plans(plans);
  return status;
}

/* Releases all TAPP objects of plans (which may be only partially created) */
void sequant_destroy_plans(sequant_plans *plans)
{
  if (plans->plan_3) TAPP_destroy_tensor_product(plans->plan_3);
  if (plans->plan_2) TAPP_destroy_tensor_product(plans->plan_2);
  if (plans->plan_1) TAPP_destroy_tensor_product(plans->plan_1);
  if (plans->info_vvoo) TAPP_destroy_tensor_info(plans->info_vvoo);
  if (plans->info_vo) TAPP_destroy_tensor_info(plans->info_vo);
  if (plans->info_scalar) TAPP_destroy_tensor_info(plans->info_scalar);
  if (plans->exec) TAPP_destroy_executor(plans->exec);
  if (plans->handle) TAPP_destroy_handle(plans->handle);
  memset(plans, 0, sizeof *plans);
}

sequant_status sequant_test_section(const sequant_plans *plans, const sequant_io *io, TAPP_error *error)
{
  sequant_status status = SEQUANT_SUCCESS;
  const double one = 1.0;
  TAPP_status tapp_status = 0;
  double *A_vo = NULL;
  double *R2_vvoo = NULL;
  double *R2u_vvoo = NULL;

  SEQUANT_ALLOCATE_ZERO(R2u_vvoo, plans->size_vvoo);
  SEQUANT_ALLOCATE(A_vo, plans->size_vo);
  SEQUANT_LOAD("A_vo", A_vo, plans->size_vo);
  /* R2u[a_1, a_2, i_1, i_2] += A[a_1, i_1] A[a_2, i_2] */
  SEQUANT_CHECK(TAPP_execute_product(plans->plan_1, plans->exec, &tapp_status,
                                     &one, A_vo, A_vo, &one, R2u_vvoo, R2u_vvoo));
  free(A_vo);
  A_vo = NULL;
  SEQUANT_STORE("R2u_vvoo", R2u_vvoo, plans->size_vvoo);
  free(R2u_vvoo);
  R2u_vvoo = NULL;

  SEQUANT_ALLOCATE_ZERO(R2_vvoo, plans->size_vvoo);
  SEQUANT_ALLOCATE(R2u_vvoo, plans->size_vvoo);
  SEQUANT_LOAD("R2u_vvoo", R2u_vvoo, plans->size_vvoo);
  /* R2[a_1, a_2, i_1, i_2] += R2u[a_1, a_2, i_1, i_2] */
  SEQUANT_CHECK(TAPP_execute_product(plans->plan_2, plans->exec, &tapp_status,
                                     &one, R2u_vvoo, &one, &one, R2_vvoo, R2_vvoo));
  /* R2[a_1, a_2, i_1, i_2] += R2u[a_2, a_1, i_1, i_2] */
  SEQUANT_CHECK(TAPP_execute_product(plans->plan_3, plans->exec, &tapp_status,
                                     &one, R2u_vvoo, &one, &one, R2_vvoo, R2_vvoo));
  free(R2u_vvoo);
  R2u_vvoo = NULL;
  SEQUANT_STORE("R2_vvoo", R2_vvoo, plans->size_vvoo);
  free(R2_vvoo);
  R2_vvoo = NULL;

cleanup:
  free(A_vo);
  free(R2_vvoo);
  free(R2u_vvoo);
  return status;
}

=================================================
